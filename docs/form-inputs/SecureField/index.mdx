---
name: SecureField
menu: Form & Inputs
route: /form-inputs/securefield
---

import ContentBlock from 'src/components/contentBlock';

# SecureField
A masked [`TextField`](/form-inputs/textfield) for passwords or other sensitive data. 

<ContentBlock imgName="">
  <code className="clike">
    {`
/* 
  struct SecureField<Label> where Label : View
*/
struct SecureField: View {
    @State private var password: String = ""\n
    var body: some View {
        Form {
            SecureField("Enter password", text: $password)\n
            SecureField("Enter password", text: $password)
                .textFieldStyle(RoundedBorderTextFieldStyle())
        }
    }
}
    `}
  </code>
</ContentBlock>